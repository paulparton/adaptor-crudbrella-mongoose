describe("the native mongoose crudbrella module", function(){

	beforeEach(function(){
		
		//Should I actuall connect to a database here?

		//Should I write something that mocks the success and error
		//responses of mongoose

	});

	describe("Create functionality", function(){

		it('creates a document', function(){

		});

	});

	describe("Read functionality", function(){

		it('populates fields of from other collections', function(){

		});

		it('reads a single document by id', function(){

		});

		it('reads one or more documents by query', function(){

		});

		it('reads all documents in a collection', function(){

		});	

		it('returns an error if the no records are found', function(){

		});

	});

	describe("Update functionality", function(){

		it("depopulates and populated fields before updating a document", function(){
			
		});

		it("idetifies a document by id and updates its String properties", function(){

		});

		it("idetifies a document by id and updates its Array properties", function(){

		});		

		it("idetifies a document by id and updates its nested Object properties", function(){

		});		
		
		it('returns an error if the document cannot be found', function(){

		});

	});

	describe("Delete functionality", function(){

		it('returns a success callback when a document is deleted', function(){
			
		});

		it('returns an error if the document cannot be found', function(){

		});

	});	




});